name: Keep Immortal Oracle Alive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Immortal Oracle
        run: curl -s https://immortal-oracle-bot.vercel.app > /dev/null
